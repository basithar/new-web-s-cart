# Server Port Configuration
PORT=5000

# MongoDB Atlas or Local Connection String
# Note: If database is not running, backend automatically falls back to an in-memory database!
MONGODB_URI=mongodb://127.0.0.1:27017/smartcart
